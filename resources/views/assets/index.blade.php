@extends('layouts.master') @section('title','Kelola Aset') @section('content')
<div class="d-flex justify-content-between mb-4"><h4 class="fw-bold">Kelola Aset</h4><button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#addAsset">Tambah Aset</button></div>
@if(session('success'))<div class="alert alert-success">{{session('success')}}</div>@endif
<div class="row g-4">@forelse($assets as $a)<div class="col-md-6 col-xl-4"><div class="card h-100">@if($a->images->count())<div id="asset{{$a->id}}" class="carousel slide"><div class="carousel-inner">@foreach($a->images as $img)<div class="carousel-item {{$loop->first?'active':''}}"><img src="{{asset('storage/'.$img->path)}}" class="d-block w-100" style="height:210px;object-fit:cover"></div>@endforeach</div>@if($a->images->count()>1)<button class="carousel-control-prev" data-bs-target="#asset{{$a->id}}" data-bs-slide="prev"><span class="carousel-control-prev-icon"></span></button><button class="carousel-control-next" data-bs-target="#asset{{$a->id}}" data-bs-slide="next"><span class="carousel-control-next-icon"></span></button>@endif</div>@endif<div class="card-body"><span class="badge bg-label-primary">{{$a->type}}</span><h5 class="mt-2">{{$a->name}}</h5><p class="small text-muted mb-1"><i class="bx bx-map"></i> {{$a->location}}</p><p class="small">Kapasitas: {{$a->capacity??'-'}} orang</p><p>{{$a->facilities}}</p><form method="POST" action="{{route('assets.destroy',$a)}}" onsubmit="return confirm('Hapus aset?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Hapus</button></form></div></div></div>@empty<div class="text-center text-muted py-5">Belum ada aset.</div>@endforelse</div>
<div class="modal fade" id="addAsset"><div class="modal-dialog modal-lg"><form class="modal-content" method="POST" enctype="multipart/form-data" action="{{route('assets.store')}}">@csrf<div class="modal-header"><h5>Tambah Aset</h5><button class="btn-close" data-bs-dismiss="modal"></button></div><div class="modal-body"><div class="row g-3"><div class="col-md-8"><label>Nama Aset</label><input name="name" class="form-control" required></div><div class="col-md-4"><label>Jenis</label><select name="type" class="form-select">@foreach(['ruangan','kendaraan','peralatan','lainnya'] as $t)<option>{{$t}}</option>@endforeach</select></div><div class="col-md-8"><label>Lokasi</label><input name="location" class="form-control" required></div><div class="col-md-4"><label>Kapasitas</label><input type="number" name="capacity" class="form-control"></div><div class="col-12"><label>Fasilitas</label><textarea name="facilities" class="form-control"></textarea></div><div class="col-12"><label>Deskripsi</label><textarea name="description" class="form-control"></textarea></div><div class="col-12"><label>Foto (bisa lebih dari satu)</label><input type="file" name="images[]" multiple accept="image/*" class="form-control"></div><div class="col-12"><label><input type="checkbox" name="is_public" value="1" checked> Tampilkan di halaman publik</label></div></div></div><div class="modal-footer"><button class="btn btn-primary">Simpan</button></div></form></div></div>
@endsection
