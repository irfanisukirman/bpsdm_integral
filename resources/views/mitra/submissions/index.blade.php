@extends('layouts.master')
@section('title','Portal Mitra')
@section('content')
<div class="d-flex flex-column flex-lg-row justify-content-between align-items-lg-center gap-3 mb-4"><div><h4 class="fw-bold mb-1">Portal Mitra</h4><p class="text-muted mb-0">Kelola usulan pelatihan dan kerja sama dalam satu ruang kolaborasi.</p></div><div class="d-flex gap-2"><a href="{{ route('mitra.submissions.create','training') }}" class="btn btn-primary"><i class="bx bx-book-add me-1"></i>Ajukan Pelatihan</a><a href="{{ route('mitra.submissions.create','cooperation') }}" class="btn btn-warning"><i class="bx bx-handshake me-1"></i>Ajukan Kerja Sama</a></div></div>
<div class="row g-3 mb-4">@foreach([['Draft',$stats['draft'],'bx-edit','secondary'],['Dalam Proses',$stats['process'],'bx-loader-circle','primary'],['Perlu Revisi',$stats['revision'],'bx-error-circle','warning'],['Final',$stats['final'],'bx-check-shield','success']] as [$label,$value,$icon,$color])<div class="col-6 col-xl-3"><div class="card border-0 shadow-sm h-100"><div class="card-body d-flex align-items-center gap-3"><span class="avatar-initial rounded bg-label-{{$color}} p-2"><i class="bx {{$icon}} fs-4"></i></span><div><h4 class="mb-0">{{$value}}</h4><small class="text-muted">{{$label}}</small></div></div></div></div>@endforeach</div>
<div class="card border-0 shadow-sm"><div class="card-header border-bottom"><h5 class="mb-0">Daftar Pengajuan</h5></div><div class="table-responsive"><table class="table table-hover align-middle"><thead class="table-light"><tr><th>Pengajuan</th><th>Bidang Tujuan</th><th>Status</th><th>Dokumen</th><th class="text-end">Aksi</th></tr></thead><tbody>@forelse($submissions as $s)<tr><td><strong class="d-block">{{$s->title}}</strong><small class="text-muted">{{$s->type_label}} · {{$s->created_at->translatedFormat('d M Y')}}</small></td><td class="text-wrap" style="max-width:280px">{{$s->target_bidang}}</td><td><span class="badge bg-label-{{ $s->status==='final'?'success':($s->status==='revision_requested'?'warning':($s->status==='draft'?'secondary':'primary')) }}">{{$s->status_label}}</span></td><td>{{$s->documents_count}} versi</td><td class="text-end"><a href="{{route('mitra.submissions.show',$s)}}" class="btn btn-sm btn-outline-primary">Buka</a></td></tr>@empty<tr><td colspan="5" class="text-center py-5 text-muted"><i class="bx bx-file-blank fs-1 d-block"></i>Belum ada pengajuan.</td></tr>@endforelse</tbody></table></div>@if($submissions->hasPages())<div class="card-footer">{{$submissions->links()}}</div>@endif</div>
@endsection